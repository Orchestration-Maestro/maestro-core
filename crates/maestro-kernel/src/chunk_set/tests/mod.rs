//! Tests of the chunk set records: their lifecycle, their chunks and the
//! guards the database holds them to, with the same outcomes on Linux, macOS
//! and Windows.

mod chunks;
mod guards;
mod lifecycle;
mod support;

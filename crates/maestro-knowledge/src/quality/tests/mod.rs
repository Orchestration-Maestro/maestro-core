//! The quality gate's unit tests: each automatic check with a document it
//! flags and one it must not, and the precedence of a decision.

mod body;
mod precedence;
mod record;
mod secrets;
mod support;

/.tools/
# Spec Kit, installed once beside the organization's checkouts (ADR-0017).
/.specify
/target/
/mutants.out*/

# Coverage runs of test children that start with a cleared environment write
# their raw profile where they run.
*.profraw
